# Ejemplo minimo — Endurance Harness Engineering

Proyecto generado con:

```powershell
.\bin\endurance-init.ps1 -Name minimal -Path .\examples\minimal -Force
```

Smoke test del scaffold. Ejecuta `./init.ps1` desde esta carpeta para validar Docker + verify.
